"use strict";
exports.__esModule = true;
var expect_more_1 = require("expect-more");
var jest_matcher_utils_1 = require("jest-matcher-utils");
var create_result_1 = require("./lib/create-result");
exports.toBeWithinRangeMatcher = function (value, floor, ceiling) {
    return create_result_1.createResult({
        message: function () {
            return "expected " + jest_matcher_utils_1.printReceived(value) + " to be greater than or equal to " + jest_matcher_utils_1.printExpected(floor) + " and less than or equal to " + jest_matcher_utils_1.printExpected(ceiling);
        },
        notMessage: function () {
            return "expected " + jest_matcher_utils_1.printReceived(value) + " not to be greater than or equal to " + jest_matcher_utils_1.printExpected(floor) + " and less than or equal to " + jest_matcher_utils_1.printExpected(ceiling);
        },
        pass: expect_more_1.isWithinRange(floor, ceiling, value)
    });
};
expect.extend({ toBeWithinRange: exports.toBeWithinRangeMatcher });
