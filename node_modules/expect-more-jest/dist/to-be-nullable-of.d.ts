/// <reference types="jest" />
declare global {
    namespace jest {
        interface Matchers<R, T> {
            /**
             * Asserts that ${value} is equal to ${other} or null.
             * @example
             * expect({ x: 12, y: 22 }).toBeNullableOf({
             *   x: expect.toBeNumber(),
             *   y: expect.toBeNumber(),
             * });
             * expect(null).toBeNullableOf({
             *   x: expect.toBeNumber(),
             *   y: expect.toBeNumber(),
             * });
             */
            toBeNullableOf(other: any): R;
        }
        interface Expect {
            /**
             * Asserts that ${value} is equal to ${other} or null.
             * @example
             * expect({ x: 12, y: 22 }).toEqual(
             *   expect.toBeNullableOf({
             *     x: expect.toBeNumber(),
             *     y: expect.toBeNumber()
             *     })
             * );
             * expect(null).toEqual(
             *   expect.toBeNullableOf({
             *     x: expect.toBeNumber(),
             *     y: expect.toBeNumber()
             *     })
             * );
             */
            toBeNullableOf<T>(other: any): JestMatchers<T>;
        }
    }
}
export declare const toBeNullableOfMatcher: (value: any, other: any) => {
    message: () => string;
    pass: boolean;
};
