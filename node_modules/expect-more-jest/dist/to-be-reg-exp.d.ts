/// <reference types="jest" />
declare global {
    namespace jest {
        interface Matchers<R, T> {
            /**
             * Asserts that ${value} is a `RegExp`.
             * @example
             * expect(new RegExp('i am a regular expression')).toBeRegExp();
             */
            toBeRegExp(): R;
        }
        interface Expect {
            /**
             * Asserts that ${value} is a `RegExp`.
             * @example
             * expect(new RegExp('i am a regular expression')).toEqual(
             *   expect.toBeRegExp()
             * );
             */
            toBeRegExp<T>(): JestMatchers<T>;
        }
    }
}
export declare const toBeRegExpMatcher: (value: any) => {
    message: () => string;
    pass: boolean; /**
     * Asserts that ${value} is a `RegExp`.
     * @example
     * expect(new RegExp('i am a regular expression')).toBeRegExp();
     */
};
