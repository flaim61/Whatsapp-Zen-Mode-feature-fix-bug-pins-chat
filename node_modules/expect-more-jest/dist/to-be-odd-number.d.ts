/// <reference types="jest" />
declare global {
    namespace jest {
        interface Matchers<R, T> {
            /**
             * Asserts that ${value} is an odd `Number`.
             * @example
             * expect(5).toBeOddNumber();
             */
            toBeOddNumber(): R;
        }
        interface Expect {
            /**
             * Asserts that ${value} is an odd `Number`.
             * @example
             * expect(5).toEqual(
             *   expect.toBeOddNumber()
             * );
             */
            toBeOddNumber<T>(): JestMatchers<T>;
        }
    }
}
export declare const toBeOddNumberMatcher: (value: any) => {
    message: () => string;
    pass: boolean;
};
