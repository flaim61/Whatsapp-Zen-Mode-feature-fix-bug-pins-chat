/// <reference types="jest" />
declare global {
    namespace jest {
        interface Matchers<R, T> {
            /**
             * Asserts that ${value} is a valid `String` containing no characters.
             * @example
             * expect('').toBeEmptyString();
             */
            toBeEmptyString(): R;
        }
        interface Expect {
            /**
             * Asserts that ${value} is a valid `String` containing no characters.
             * @example
             * expect('').toEqual(
             *   expect.toBeEmptyString()
             * );
             */
            toBeEmptyString<T>(): JestMatchers<T>;
        }
    }
}
export declare const toBeEmptyStringMatcher: (value: any) => {
    message: () => string;
    pass: boolean;
};
