/// <reference types="jest" />
declare global {
    namespace jest {
        interface Matchers<R, T> {
            /**
             * Asserts that ${value} is `false` or `new Boolean(false)`.
             * @example
             * expect(false).toBeFalse();
             */
            toBeFalse(): R;
        }
        interface Expect {
            /**
             * Asserts that ${value} is `false` or `new Boolean(false)`.
             * @example
             * expect(false).toEqual(
             *   expect.toBeFalse()
             * );
             */
            toBeFalse<T>(): JestMatchers<T>;
        }
    }
}
export declare const toBeFalseMatcher: (value: any) => {
    message: () => string;
    pass: boolean; /**
     * Asserts that ${value} is `false` or `new Boolean(false)`.
     * @example
     * expect(false).toBeFalse();
     */
};
