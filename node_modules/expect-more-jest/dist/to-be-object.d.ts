/// <reference types="jest" />
declare global {
    namespace jest {
        interface Matchers<R, T> {
            /**
             * Asserts that ${value} is an `Object`.
             * @example
             * expect({}).toBeObject();
             */
            toBeObject(): R;
        }
        interface Expect {
            /**
             * Asserts that ${value} is an `Object`.
             * @example
             * expect({}).toEqual(
             *   expect.toBeObject()
             * );
             */
            toBeObject<T>(): JestMatchers<T>;
        }
    }
}
export declare const toBeObjectMatcher: (value: any) => {
    message: () => string;
    pass: boolean; /**
     * Asserts that ${value} is an `Object`.
     * @example
     * expect({}).toBeObject();
     */
};
